{
  "name": "mono-prototype",
  "version": "0.0.0",
  "description": "Mono Prototype",
  "private": true,
  "scripts": {
    "client": "cd packages/client && yarn start",
    "client:dev": "cd packages/client && yarn start:dev",
    "db": "cd packages/server && yarn db",
    "server": "cd packages/client && yarn start:dev",
    "server:dev": "cd packages/client && yarn start:dev"
  },
  "workspaces": [
    "packages/*"
  ]
}
